[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    reduction: slow reduction pipeline tests (requires raw FITS files and pipeline config)
